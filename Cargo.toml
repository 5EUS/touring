[package]
name = "touring"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

reqwest = { version = "0.12", features = ["json"] }
tokio = { version = "1", features = ["full"] }

scraper = "0.24.0"

anyhow = "1.0"

wasmtime = "36.0.2"

wit-bindgen = "0.45.0"
